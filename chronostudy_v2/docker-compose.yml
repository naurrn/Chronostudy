version: "3.9"

services:
  chronostudy:
    build: .
    container_name: chronostudy
    restart: unless-stopped

    ports:
      - "8000:8000"

    volumes:
      - ./backend:/app/backend
      - ./frontend:/app/frontend